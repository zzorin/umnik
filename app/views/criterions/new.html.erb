<%= simple_form_for @criterion, url: :contest_criterions do |f| %>
  <%= f.input :title %>
  <%= f.button :submit, class: 'btn-green' %>
<% end %>

<%= link_to 'Отменить', contest_criterions_path, class: 'btn btn-light-blue' %>
